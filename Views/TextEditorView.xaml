<UserControl x:Class="CopyChanges.Views.TextEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewmodels="clr-namespace:CopyChanges.ViewModels">
    <UserControl.DataContext>
        <viewmodels:MainViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Content="Browse Project Directory" Command="{Binding BrowseProjectDirectoryCommand}" />
            <Button Content="Get Git Changes (Editor 1)" Command="{Binding GetGitChangesCommand}" />
            <TextBlock Text="{Binding StatusMessage}" Margin="10,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
        <Grid Grid.Row="1">
            <!-- Define rows and columns for editors -->
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Place TextEditors -->
            <ItemsControl ItemsSource="{Binding TextEditors}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="3" Columns="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <TextBlock Text="Editor" FontWeight="Bold" />
                            <TextBox Text="{Binding Content}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,20,0,0" />
                            <Button Content="Copy Content" Command="{Binding CopyContentCommand}" VerticalAlignment="Bottom" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
